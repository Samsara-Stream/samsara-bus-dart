name: samsara_bus
description: RxDart-based multi-topics message/event bus.
version: 1.0.1
repository: https://github.com/Samsara-Stream/samsara-bus-dart

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  rxdart: ^0.28.0
  uuid: ^4.1.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
